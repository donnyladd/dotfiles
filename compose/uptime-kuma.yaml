

services:
  uptime-kuma:
    image: louislam/uptime-kuma
    container_name: uptime-kuma
    ports:
      - '3001:3001'
    volumes:
      - 'uptime-kuma:/app/data'
    restart: unless-stopped

volumes:
  uptime-kuma:
    external: true
